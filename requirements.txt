# Verified working set (Python 3.10, Apple Silicon / MPS). Pins reflect the
# exact versions used to produce results/. CPU/MPS only; no CUDA required.
torch==2.5.1
torchvision==0.20.1
ultralytics==8.3.27
opencv-python==4.10.0.84
numpy>=1.26,<3
scikit-learn>=1.5
matplotlib>=3.9
seaborn>=0.13
pandas>=2.2
Pillow>=10
tqdm>=4.66

# Notes
# -----
# * Training currently runs on CPU: ultralytics 8.3.27 + torch 2.5.1 hits a
#   shape-mismatch bug in the task-aligned assigner on the MPS backend. Inference
#   runs fine on MPS or CPU. See docs/METHODOLOGY.md.
# * pycocotools is optional (only for cross-checking the built-in evaluator).
