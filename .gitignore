# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
env/
ENV/
.venv/

# PyTorch Models
*.pt
*.pth
*.ckpt

# Jupyter Notebooks
.ipynb_checkpoints

# Dataset files
*.zip
*.tar.gz
*.rar

# Large result directories
results/detection_results/
results/enhanced_images/

# Regenerable / large research artifacts
# (NOTE: .gitignore does NOT support trailing inline comments — keep them on
#  their own lines, or the whole pattern is treated as literal and ignores nothing.)
# YOLO-format export (symlinks + labels), regenerable from scripts/00_make_split.py
data/exdark_yolo/
# enhanced-domain exports (materialised enhanced images), regenerable; large
data/exdark_yolo_*/
# large path manifest; the JSON split is committed instead
data/splits/*.csv
# ultralytics training/val outputs (weights, plots, caches) — regenerable
runs/
# ultralytics label caches
*.cache
# logs
/tmp/*.log
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# Temporary files
*.tmp
*.temp